name: Build ETF snapshot

on:
  schedule:
    - cron: "0 7 * * *"   # 16:00 KST (UTC+9) · 장마감(15:30 KST) 30분 후 · VPS cron 장애 시 fallback
  workflow_dispatch:

concurrency:
  group: snapshot-etf
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write   # gh workflow run deploy.yml 호출용
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build snapshot (D-1 종가 기준)
        run: python build_snapshot.py

      - name: Commit & push if changed
        id: commit
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add etf_data.parquet
          if git diff --cached --quiet; then
            echo "No snapshot changes."
            echo "pushed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git commit -m "data: ETF 스냅샷 갱신 ($(TZ=Asia/Seoul date +%Y-%m-%dT%H:%M))"
          git push
          echo "pushed=true" >> "$GITHUB_OUTPUT"

      # GITHUB_TOKEN으로 푸시한 커밋은 deploy.yml의 on:push를 트리거하지 못하므로
      # 변경이 있을 때 workflow_dispatch로 직접 deploy를 호출한다.
      - name: Trigger deploy
        if: steps.commit.outputs.pushed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run deploy.yml --ref main
